# Implementation Plans

Generated by the improve skill on 2026-06-17 and reconciled after the
dependency/tooling migration commit `b2bc41e`. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Upgrade all dependencies to current latest and verify with Vercel env | P1 | L | - | DONE (`b2bc41e`) |
| 002 | Restore green quality gates after the Bun/Biome migration | P1 | M | 001 | TODO |
| 003 | Add a minimal regression test harness for mutation APIs | P1 | M | 002 | TODO |
| 004 | Consolidate the post-shadcn UI primitive layer | P2 | L | 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 is marked DONE because the stack migration landed in commit `b2bc41e`:
  Bun lockfile, Tailwind v4, Biome, Kumo, and `proxy.ts` are now in the branch.
- 002 should run before more refactors because `bun run lint` currently fails;
  later plans need a clean quality gate.
- 003 depends on 002 so the new test files start from a stable lint/typecheck
  baseline.
- 004 can run after 002, but it should not run before 003 if the same executor
  plans to touch API mutation flows.

## Findings considered and rejected

- Full Kumo replacement of every Ant Design admin screen in one pass: rejected
  for now because the admin tables/forms depend heavily on Ant Design behavior.
  Plan 004 narrows the first UI pass to primitive boundaries and leaves large
  table/form migrations for a later, tested slice.
- Enabling `strict: true` immediately: rejected as a first follow-up because
  the repo still has widespread `any` and generated/CMS shapes. Tighten
  targeted boundaries first, then revisit strict mode after tests exist.
- Removing `sonner` in favor of a Kumo toast immediately: rejected because
  existing upload/editor/admin flows call the `toast.*` API directly. Keeping
  Sonner is lower risk until UI primitives have a clear adapter layer.
