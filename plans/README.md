# Implementation Plans

Generated by the improve skill on 2026-06-17. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Upgrade all dependencies to current latest and verify with Vercel env | P1 | L | — | BLOCKED (`latest` upgrade path is incompatible as-is: `@tremor/react` still peers on React 18, and the Next 16 lint stack does not accept `eslint@10`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 has no plan dependencies.

## Findings considered and rejected

- Directly pulling Vercel environment values during this advisor pass: rejected
  because the improve skill is read-only and plan files must never contain
  secret values. The execution plan instead tells the implementer how to pull
  Vercel env into ignored local files and verify variable names only.
- Directly upgrading dependencies during this advisor pass: rejected because
  the improve skill forbids source edits and install/build commands that mutate
  the user's working tree. The execution plan scopes the required edits and
  verification gates for a separate executor.
- Blindly upgrading every dependency to the newest major in one pass: rejected
  after execute review because the first install already surfaced ecosystem
  incompatibilities (`@tremor/react` vs React 19, and Next 16 lint packages vs
  ESLint 10). The next pass should target a compatibility-constrained upgrade,
  not unconditional `latest`.
